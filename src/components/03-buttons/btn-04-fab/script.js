/**
 * MÓDULO AUTÓNOMO: Botones Flotantes (FAB)
 *
 * El FAB simple no requiere lógica JavaScript,
 * pero se incluye este archivo para mantener la arquitectura
 * modular del componente (HTML/CSS/JS/Snippet).
 *
 * Si se implementa un submenú expandible, la lógica de toggle
 * y la rotación del ícono (+) irían aquí.
 */
document.addEventListener("DOMContentLoaded", () => {
    // Lógica futura de expansión del menú (si aplica)
});
