<button 
    id="emitter-btn" 
    class="btn btn-warning"
>
    Disparar Evento Global
</button>

<div id="listener-box" class="listener-box border p-3">
    <p class="m-0 text-bold text-center">
        Estatus del Receptor
    </p>
    <p id="listener-output" class="m-b-0 text-center">
        Esperando un evento...
    </p>
</div>

<script>
// Lógica del Emisor:
// 1. Usa la utilidad global: window.UI_KIT_EVENTS.emit('nombre:evento', {datos: ...});
// 2. Por ejemplo:
// document.getElementById('emitter-btn').addEventListener('click', () => {
//     window.UI_KIT_EVENTS.emit('custom:pedido-listo', { id: 1234 });
// });

// Lógica del Receptor:
// 1. Usa la utilidad global: window.UI_KIT_EVENTS.on('nombre:evento', (e) => { ... });
// 2. Por ejemplo:
// window.UI_KIT_EVENTS.on('custom:pedido-listo', (e) => {
//     console.log('Pedido listo con ID:', e.detail.id);
// });
</script>